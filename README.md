# new-profile-generator
the user will be prompted with two questions first of which is the user's favourite color, then the user's github's user name.
then a pdf will be generated based on those results.
